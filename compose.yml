services:
  app:
    build: .
    volumes:
      - .:/app
    command: python3 -m pytest
  database:
    image: postgres:latest
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
